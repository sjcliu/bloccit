<%= form_for(@wonton) do |f| %>
  <% if @wonton.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wonton.errors.count, "error") %> prohibited this wonton from being saved:</h2>

      <ul>
      <% @wonton.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :copy %><br>
    <%= f.text_area :copy %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.number_field :price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
